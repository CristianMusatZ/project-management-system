# ========================================
# Project Management System - Environment
# ========================================
# Copiază acest fișier ca .env și completează valorile

# PostgreSQL
POSTGRES_DB=pms_db
POSTGRES_USER=pms_user
POSTGRES_PASSWORD=pms_secret_2024

# MongoDB
MONGO_USER=pms_mongo
MONGO_PASSWORD=pms_mongo_2024
MONGO_DB=pms_projects

# JWT Authentication
JWT_SECRET=super_secret_jwt_key_change_in_production
JWT_EXPIRES_IN=1d

# Backend
NODE_ENV=development
PORT=4000

# Frontend
VITE_API_URL=http://localhost:4000/api

# ─────────────────────────────────────────────────────
# SMTP — Notificări Email (opțional)
# Dacă SMTP_HOST, SMTP_USER și SMTP_PASS nu sunt setate,
# emailurile sunt dezactivate (notificările in-app funcționează oricum).
# ─────────────────────────────────────────────────────
# Gmail:      SMTP_HOST=smtp.gmail.com  SMTP_PORT=587  SMTP_SECURE=false
# Office 365: SMTP_HOST=smtp.office365.com  SMTP_PORT=587  SMTP_SECURE=false
# Outlook:    SMTP_HOST=smtp-mail.outlook.com  SMTP_PORT=587  SMTP_SECURE=false
# SendGrid:   SMTP_HOST=smtp.sendgrid.net  SMTP_PORT=587  SMTP_SECURE=false  SMTP_USER=apikey  SMTP_PASS=<API_KEY>
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@exemplu.ro
SMTP_FROM_NAME=Project Management System
